# Web-
A cross-platform GUI tool for batch testing URL accessibility, supporting concurrent requests, timeout control, automatic retries, and result export (CSV/JSON). 一款跨平台 GUI 批量 URL 访问检测工具，支持并发请求、超时控制、自动重试及结果导出（CSV/JSON）。
